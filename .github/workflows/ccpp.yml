name: srsRAN-Project Statik Derleme

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Kodu Çek
        uses: actions/checkout@v4

      - name: Bağımlılıkları Yükle
        run: |
          sudo apt update
          sudo apt install -y cmake make gcc g++ \
            libfftw3-dev libmbedtls-dev libsctp-dev \
            libzmq3-dev libczmq-dev libconfig++-dev \
            libyaml-cpp-dev libgtest-dev \
            libunwind-dev libdw-dev libboost-program-options-dev

      - name: Derleme Klasörünü Oluştur
        run: mkdir build

      - name: CMake Yapılandırması (Statik Mod)
        working-directory: ./build
        run: |
          cmake ../ -DENABLE_ZEROMQ=ON \
                    -DENABLE_STATIC=ON \
                    -DCMAKE_BUILD_TYPE=Release \
                    -DENABLE_WERROR=OFF

      - name: Derlemeyi Başlat (Make)
        working-directory: ./build
        run: make -j$(nproc)

      - name: Dosyaları Paketle ve Yükle (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: srsRAN-Project-Static-Binaries
          path: |
            build/apps/gnb/gnb
            build/apps/ue/ue
          if-no-files-found: error
